diff --git a/shadowhook/src/main/cpp/shadowhook.c b/shadowhook/src/main/cpp/shadowhook.c
--- a/shadowhook/src/main/cpp/shadowhook.c
+++ b/shadowhook/src/main/cpp/shadowhook.c
@@ -87,7 +87,8 @@ int shadowhook_init(shadowhook_mode_t default_mode, bool debuggable) {
       sh_island_init();
       sh_enter_init();
       sh_switch_init();
-      if (__predict_false(0 != sh_linker_init())) GOTO_END(SHADOWHOOK_ERRNO_INIT_LINKER);
-      if (__predict_false(0 != sh_task_init())) GOTO_END(SHADOWHOOK_ERRNO_INIT_TASK);
+      // OfflineCore hooks verified absolute addresses after libil2cpp is fully
+      // loaded. The linker constructor monitor and queued symbol task module
+      // are neither required nor safe on the target Samsung linker.
 
 #undef GOTO_END
